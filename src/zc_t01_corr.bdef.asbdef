projection;
strict ( 2 );
use draft;
extensible;
use side effects;
define behavior for ZC_T01_CORR alias ZcT01Corr
extensible
use etag
{
  use create;
  use update;
  use delete;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;
}